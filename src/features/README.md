# Feature modules

Use this folder for feature-specific code that grows beyond a single route or component.

Suggested modules:

- `names` - search, filters, name cards, detail state
- `numerology` - calculation forms and result UI
- `payments` - premium unlock and Razorpay checkout flows
- `consultations` - WhatsApp lead capture and follow-up flows
