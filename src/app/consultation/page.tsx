import { FeaturePlaceholder } from "@/components/layout/feature-placeholder";

export default function ConsultationPage() {
  return (
    <FeaturePlaceholder
      description="This route will capture consultation leads and connect parents to WhatsApp follow-up."
      eyebrow="WhatsApp"
      title="Consultation funnel"
    />
  );
}
